# 📤 Инструкция по загрузке проекта в GitHub

## Вариант 1: Через PowerShell скрипт (рекомендуется)

### Шаг 1: Установите Git
Если Git не установлен:
1. Скачайте с https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Перезапустите терминал

### Шаг 2: Настройте Git
```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

### Шаг 3: Запустите скрипт
```powershell
.\upload_to_github.ps1
```

Скрипт автоматически:
- ✅ Найдет проект
- ✅ Инициализирует Git (если нужно)
- ✅ Добавит все файлы
- ✅ Создаст коммит
- ✅ Настроит remote репозиторий
- ✅ Загрузит код в GitHub

---

## Вариант 2: Вручную через командную строку

### Шаг 1: Найдите путь к проекту
```powershell
Get-ChildItem -Path Z:\ -Recurse -Filter "codemagic.yaml" | Select-Object DirectoryName
```

### Шаг 2: Перейдите в директорию проекта
```powershell
cd "путь_к_проекту"
```

### Шаг 3: Выполните команды
```bash
# Инициализация (если еще не сделано)
git init

# Добавление файлов
git add .

# Коммит
git commit -m "Initial commit: Kasidie City Whisper iOS app"

# Настройка remote
git remote add origin https://github.com/fryfarrrell/testrep.git

# Переименование ветки
git branch -M main

# Загрузка в GitHub
git push -u origin main
```

**Примечание**: При `git push` вам нужно будет:
- Ввести логин GitHub
- Ввести пароль (или Personal Access Token)

---

## Вариант 3: Через GitHub Desktop

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Установите и войдите в GitHub
3. File → Add Local Repository
4. Выберите папку проекта
5. Нажмите "Publish repository"
6. Выберите репозиторий `fryfarrrell/testrep`

---

## Вариант 4: Через веб-интерфейс GitHub

1. Откройте https://github.com/fryfarrrell/testrep
2. Нажмите на синюю ссылку **"uploading an existing file"**
3. Перетащите все файлы проекта в окно
4. Внизу введите: "Initial commit"
5. Нажмите **"Commit changes"**

**Важно**: Убедитесь, что загрузили файл `codemagic.yaml`!

---

## 🔐 Настройка Personal Access Token (для Git)

Если Git требует пароль, используйте Personal Access Token:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен вместо пароля

---

## ✅ После загрузки

1. Проверьте репозиторий: https://github.com/fryfarrrell/testrep
2. Убедитесь, что файл `codemagic.yaml` есть в корне
3. Перейдите на Codemagic и подключите репозиторий

---

## 🐛 Решение проблем

### "Git не найден"
→ Установите Git и перезапустите терминал

### "Permission denied"
→ Проверьте права доступа к репозиторию на GitHub

### "Authentication failed"
→ Используйте Personal Access Token вместо пароля

### "Path not found"
→ Убедитесь, что путь к проекту правильный

---

**Готово!** После загрузки кода можно подключать к Codemagic! 🚀
