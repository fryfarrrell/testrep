# 📦 Инструкция: Загрузка в GitHub с портативным Git

## 🚀 Быстрый старт

### Вариант 1: Автоматический скрипт (рекомендуется)

1. Запустите скрипт:
   ```powershell
   .\upload_to_github_portable.ps1
   ```

2. Скрипт автоматически:
   - ✅ Найдет портативный Git
   - ✅ Найдет проект
   - ✅ Настроит Git (если нужно)
   - ✅ Загрузит код в GitHub

3. При запросе введите:
   - Имя для Git (если еще не настроено)
   - Email для Git (если еще не настроено)
   - Логин GitHub
   - Пароль или Personal Access Token

### Вариант 2: Вручную через Git Bash

1. Найдите `git-bash.exe` в папке с портативным Git
2. Запустите `git-bash.exe`
3. Выполните команды:

```bash
# Найдите путь к проекту (замените на ваш путь)
cd /z/TN/Удаленка/RND/APPS/KCD

# Или используйте путь через поиск
cd "$(find /z -name "codemagic.yaml" -type f | head -1 | xargs dirname)"

# Инициализация
git init

# Настройка (если еще не сделано)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"

# Добавление файлов
git add .

# Коммит
git commit -m "Initial commit: Kasidie City Whisper iOS app"

# Настройка remote
git remote add origin https://github.com/fryfarrrell/testrep.git

# Переименование ветки
git branch -M main

# Загрузка
git push -u origin main
```

## 🔐 Настройка Personal Access Token

Если Git требует пароль, используйте токен:

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите scope: `repo` (полный доступ)
4. Скопируйте токен
5. При `git push` используйте:
   - Username: ваш логин GitHub
   - Password: вставьте токен (не пароль!)

## 📍 Где находится портативный Git?

Обычно портативный Git находится в:
- `C:\PortableGit\`
- `D:\PortableGit\`
- `%USERPROFILE%\Downloads\PortableGit\`
- `%USERPROFILE%\Desktop\PortableGit\`

Внутри папки должен быть:
- `bin\git.exe` - основной исполняемый файл
- `git-bash.exe` - оболочка Bash

## ✅ После загрузки

1. Проверьте: https://github.com/fryfarrrell/testrep
2. Убедитесь, что файл `codemagic.yaml` есть
3. Подключите к Codemagic!

---

**Готово!** 🎉
