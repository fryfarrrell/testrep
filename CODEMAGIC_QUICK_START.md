# ⚡ Быстрый старт: Codemagic для github.com/fryfarrrell/testrep

## 🎯 За 5 минут до первой сборки

### 1. Войдите на Codemagic
👉 [codemagic.io](https://codemagic.io) → **Sign up** / **Login**

### 2. Подключите репозиторий
1. Нажмите **"Add application"**
2. Выберите **GitHub**
3. Найдите **`fryfarrrell/testrep`**
4. Выберите **Flutter** как тип проекта
5. Codemagic автоматически найдет `codemagic.yaml` ✅

### 3. Запустите сборку
1. Нажмите **"Start new build"**
2. Выберите workflow: **ios-test-workflow** (для тестирования)
3. Выберите ветку: **main** (или вашу рабочую ветку)
4. Нажмите **"Start build"**

### 4. Наблюдайте за сборкой
- Смотрите логи в реальном времени
- Дождитесь завершения (обычно 5-10 минут)
- При успехе получите уведомление на email

## 📋 Что происходит при сборке

1. ✅ Устанавливаются зависимости (`flutter pub get`)
2. ✅ Генерируется код для Hive (`build_runner`)
3. ✅ Устанавливаются CocoaPods
4. ✅ Запускаются тесты
5. ✅ Собирается iOS приложение
6. ✅ Запускается на симуляторе (для ios-test-workflow)

## 🔧 Настройка Google Maps (опционально)

Если используете Google Maps:

1. Получите API key на [Google Cloud Console](https://console.cloud.google.com/)
2. В Codemagic: **Settings** → **Environment variables**
3. Добавьте:
   - Key: `GOOGLE_MAPS_API_KEY`
   - Value: ваш API key
   - Secure: ✅

## 🐛 Частые проблемы

### "Build failed" на этапе pod install
**Решение**: Проверьте, что `ios/Podfile` существует и правильный

### "No tests found"
**Решение**: Это нормально, если тестов нет. Workflow продолжит работу

### "Simulator not found"
**Решение**: Codemagic автоматически выберет доступный симулятор

## 📱 Что дальше?

После успешной сборки:
- ✅ Приложение собрано и протестировано
- ✅ Можно скачать `.app` файл
- ✅ Можно настроить автоматическую сборку при каждом push

## 🔄 Автоматические сборки

Чтобы сборка запускалась автоматически:

1. В настройках приложения включите **"Automatic builds"**
2. Выберите ветки для автоматической сборки
3. Теперь каждый push будет запускать сборку автоматически!

---

**Готово!** 🎉 Ваше приложение теперь собирается на Codemagic!

Нужна помощь? Смотрите полную инструкцию в `CODEMAGIC_SETUP.md`
